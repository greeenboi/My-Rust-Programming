fn main() {
    println!("Hello, world!");
    let mut x = 1.1;
    x=1.12;
    let mut y = 2.2;
    y=2.22;
    print!("{} + {} = {}", x, y, x + y);
    print!("{} * {} = {}", x, y, x * y);
    // panic!("oh no!!! {}", "something went wrong");

}
