fn main() {
    println!("Hello, world!");
    let mut x = 1.1;
    let mut y = 2.2;
    print!("{} + {} = {}", x, y, x + y);
    x=1.12;
    y=2.0;
    print!("{} * {} = {}", x, y, x * y);
    // panic!("oh no!!! {}", "something went wrong");

}
